<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>View Products</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background-color: #f9e5ea; /* Soft Pink Marble */
    }
    .product-card {
      background: white;
      border: 1px solid #d4af37;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(212, 175, 55, 0.2);
      margin-bottom: 30px;
      padding: 20px;
      transition: transform 0.3s ease;
    }
    .product-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 16px rgba(212, 175, 55, 0.3);
    }
    .product-image {
      width: 100%;
      height: auto;
      border-radius: 10px;
    }
    .price {
      color: #d4af37;
      font-weight: bold;
      font-size: 1.2rem;
    }
    .original-price {
      text-decoration: line-through;
      color: gray;
      margin-left: 10px;
    }
    .discount {
      background-color: #d4af37;
      color: white;
      padding: 3px 8px;
      border-radius: 5px;
      font-size: 0.9rem;
      margin-left: 10px;
    }
    .product-title {
      margin-top: 15px;
      font-size: 1.4rem;
      font-weight: bold;
      color: #6c757d;
    }
  </style>
</head>

<body>

    <nav class="navbar navbar-expand-lg custom-navbar">
        <div class="container d-flex">
          <a class="navbar-brand" href="#">
            <img src="/images/WhatsApp Image 2025-04-28 at 23.50.36_74b5c99d.jpg" alt="Aarani" width="70" height="58" class="d-inline-block align-text-top">
          </a>
    
          <div class="dropdown ms-auto">
            <button class="btn btn-secondary " type="button" aria-expanded="false">
              Add products
            </button>
            
          </div>
        </div>
      </nav>

  <div class="container py-5">
    <h1 class="text-center mb-5" style="color: #d4af37;">Our Collections</h1>
    <div id="productsList"></div>
  </div>

  <div class="mt-32">
    <a class="block w-fit mb-2" href="">Filter by :</a>
    <a class="block w-fit mb-2" href="">Availability</a>
    <a class="block w-fit mb-2" href="">Discount</a>
</div>

<div class="w-[75%] flex flex-col gap-5 h-screen">
    <div class="flex items-start gap-5">
       <% products.forEach(function(product){ %>
            <div class="w-60 bg-red-500 ">
                <div class="w-full h-52 bg-[<%= product.bgcolor %>]">
                    <img class="w-full h-full " src="data:image/jpeg;base64,<%= product.image.toString('base64') %>" alt="">
                </div>
                <div class="flex items-center justify-between bg-[<%= product.panelcolor %>] px-4 py-4 text-[<%= product.textcolor %>]">
                    <div>
                        <h3><%=product.name %></h3>
                        <h4>Rs.<%=product.price %> </h4>
                    </div>
                    <a class="w-7 h-7 flex items-center justify-center rounded-full bg-white" href="/addtocart/<%= product._id %>"><i class="ri-add-line"></i></a>
                    
                </div>
                
            </div>
            <% }) %>
            
    </div>
</div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/js/bootstrap.bundle.min.js"></script>

 

</body>
</html>
